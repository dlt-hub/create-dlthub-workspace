[project]
name = "dlthub-minimal-workspace"
version = "0.1.0"
description = "Minimal dltHub workspace"
requires-python = ">=3.12"
dependencies = [
    "dlt[hub, workspace]>=1.17.0",
    "duckdb<1.4.3",
]

[tool.uv.sources]
