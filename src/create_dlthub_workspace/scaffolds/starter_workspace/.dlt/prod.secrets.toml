# Production secrets -- this file is gitignored. Never commit real values.
#
# MotherDuck warehouse (prod environment):
#   1) Sign up + grab a token at https://app.motherduck.com/
#   2) Uncomment the two lines below
#   3) Paste your token between the quotes
#
# The database name lives in .dlt/prod.config.toml (default: "my_db"),
# and MotherDuck auto-creates it on first connect -- no extra setup needed.

# [destination.warehouse.credentials]
# password = "<your-motherduck-token>"
