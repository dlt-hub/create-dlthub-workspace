# Production secrets -- this file is gitignored. Never commit real values.
#
# MotherDuck warehouse (prod environment):
#   1) Sign up + grab a token at https://app.motherduck.com/settings/tokens
#   2) Uncomment the three lines below
#   3) Paste your token between the quotes for `password`
#   4) Set `database` to one of your MotherDuck databases

# [destination.warehouse.credentials]
# database = "<your-motherduck-db-name>"
# password = "<your-motherduck-token>"
